# Core ML
torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.27.0
tokenizers>=0.19.0

# Training utilities
scikit-learn>=1.4.0
tqdm>=4.66.0
psutil>=5.9.0

# Numerical / analysis
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.8.0

# Notebook
jupyter>=1.0.0
ipywidgets>=8.1.0

# Metrics
torchmetrics>=1.3.0

# Data pipeline dependencies (previously missing)
beautifulsoup4>=4.12.0   # ScientificTextCleaner uses BeautifulSoup for HTML cleaning
contractions>=0.1.73     # ScientificTextCleaner uses contractions.fix()
lxml>=5.1.0              # Recommended HTML parser backend for BeautifulSoup

# Tokenizer backends (required for BioBERT/SciBERT SentencePiece tokenizers)
protobuf>=4.25.0
sentencepiece>=0.2.0

# Optional — quantisation support on CUDA
bitsandbytes>=0.43.0